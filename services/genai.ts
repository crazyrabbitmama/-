// This service is currently unused as per user request for instant feedback.
// Keeping basic structure in case we want to re-enable text generation later.

export async function generateEventImage(eventTitle: string, eventDescription: string): Promise<string | null> {
  // Disabled for faster UX
  return null;
}
